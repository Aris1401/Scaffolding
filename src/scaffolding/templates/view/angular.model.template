export class #{class_name_camel} {
    #{for} #{fields}
    #{fields:columnName}: string = ""
    #{endfor}
    #{for} #{foreign_keys}
    #{foreign_keys:columnTypeProcessed} : any = ""
    #{endfor}
}